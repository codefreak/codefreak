describe('Workspace editor', () => {
  it('edits file contents', () => {
    // TODO implement
  })

  it('saves contents on `cmd + s`', () => {
    // TODO implement
  })
})

// Convert this into a module instead of a script
export {}
