query GetAssignmentList {
  assignments {
    id
    title
    tasks {
      id
    }
  }
}

query GetAssignment($id: ID!) {
  assignment(id: $id) {
    id
    title
    tasks {
      id
      title
      position
      evaluationSteps {
        index
        runnerName
      }
    }
  }
}
